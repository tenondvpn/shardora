// SPDX-License-Identifier: GPL-2.0-or-later
pragma solidity ^0.8.20;

// LowGasSafeMath is no longer needed in Solidity 0.8.x as overflow checks are built-in.
// This file is kept as a stub for import compatibility.
// All arithmetic in 0.8.x reverts on overflow/underflow by default.
// Use `unchecked { }` blocks where intentional wrapping is needed.
library LowGasSafeMath {
    // Stub - not used in 0.8.x, arithmetic is safe by default
}
