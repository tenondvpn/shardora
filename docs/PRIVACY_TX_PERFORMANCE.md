# 隐私交易方案性能分析

> 基于系统实际参数定量分析，对比现有隐私方案的各项性能指标

---

## 一、系统基础参数（从代码中提取）

```
kRotationPeriod            = 600 s     （epoch 周期）
kTimeBlockCreatePeriodSeconds = 570 s  （timeblock 周期）
kLeaderRotationPeriodSeconds = 10 s    （HotStuff 出块周期，即 1 共识轮次）
kMaxTxCount                = 20,480    （每块最大交易数）
kMaxProposeMsgBytes        = 1 MB      （Propose 消息大小上限）
kBlockMaxGasLimit          = 6×10^16  （实际无 gas 上限约束）
kImmutablePoolSize         = 32        （每分片并行池数）
t = ⌈2n/3⌉                            （BFT 阈值，n=100 → t=67）
```

---

## 二、各阶段延迟拆解

### 2.1 普通跨分片转账延迟（基准）

```
阶段                        时间
─────────────────────────────────────────
源分片 HotStuff 共识出块     ~10 s    （kLeaderRotationPeriodSeconds）
跨分片消息路由（GBP 投递）   ~5  s    （消息传播 + 缓冲池入队）
目标分片 HotStuff 共识出块   ~10 s
─────────────────────────────────────────
总计                         ~25 s    （基准）
```

### 2.2 隐私转账新增开销拆解

#### A. 客户端 Groth16 证明生成（链外，用户侧）

证明时间与电路约束数量正比：

| 电路模块 | R1CS 约束估算 |
|---------|-------------|
| Merkle 路径验证（深度 20，Poseidon hash） | ~40,000 |
| Pedersen 承诺计算（×3） | ~4,500 |
| Nullifier 计算（Poseidon hash） | ~2,000 |
| 金额守恒约束 | ~500 |
| 64-bit 范围约束（二进制分解） | ~4,000 |
| 一次性地址关联约束 | ~3,000 |
| **合计** | **~54,000** |

对比参考：
- Tornado Cash：~28,000 constraints → bellman(Rust) ~1 s
- Zcash Sapling：~2,000,000 constraints → bellman ~2-3 s（现代 CPU）

**本方案电路约束 ~54,000**，Proving time 估算：

| 环境 | 估算时间 |
|------|---------|
| 桌面 CPU（bellman/arkworks Rust） | **1-2 s** |
| GPU（CUDA，bellman-cuda） | **~200 ms** |
| 移动端（ARM，软件实现） | **5-15 s** ⚠️ |
| 证明代理服务 | **<500 ms**（网络延迟） |

> **关键**：证明生成在链外进行，可与源分片等待确认（10s）并行，**不增加 E2E 关键路径延迟**（证明在源分片出块前就能完成）。

#### B. 目标分片：ElGamal 阈值解密（链内，委员会节点执行）

```
每个节点 i 计算：D_i = sk_i × C1    （G1 标量乘法）
  └── 单次 BN254 G1 scalar mul：~1 ms / 节点

Leader 收集 t=67 份后 Lagrange 重建：
  └── 67 次 G1 scalar mul + 加法：~70 ms

网络往返（广播 D_i，收集 t 份）：~200-500 ms
─────────────────────────────────────────────
阈值解密总耗时：~300-600 ms
```

**与现有 BFT 流程的融合方式**：

HotStuff 的 VoteMsg 收集阶段（leader 等待 t 个投票）已有 ~200-500ms 等待窗口。节点在广播 VoteMsg 的同时附带 `D_i`（仅 64 字节 G1 点），leader 在完成 QC 重建的同时完成解密重建。

**结论：阈值解密几乎完全隐藏在现有共识等待窗口内，目标分片不增加共识轮次。**

#### C. ZK 证明链上验证（EVM 执行，目标分片出块时）

Groth16 验证需要 3 次 BN254 配对 + 若干 G1/G2 乘法：

| 操作 | EVM 等效 Gas | 时间估算 |
|------|------------|---------|
| ECPAIRING（每对） | ~34,000 gas / 对 | ~10 ms / 对 |
| 完整 Groth16 verify（6 公开输入） | ~250,000 gas | ~80-100 ms |

当前系统 `kBlockMaxGasLimit` 实际无上限约束，验证在一个块内完成。

**结论：ZK 验证 ~100ms，在 EVM 执行阶段完成，不占用共识时间。**

### 2.3 E2E 延迟汇总对比

```
阶段                    普通跨分片    隐私跨分片      增量
──────────────────────────────────────────────────────────
客户端准备               ~0 s         ~1-2 s       +1-2 s（可并行）
源分片共识出块            ~10 s        ~10 s         0
跨分片路由               ~5 s         ~5 s          0
目标分片阈值解密          0            ~0.3-0.6 s   +0.3-0.6 s（藏入共识轮）
目标分片 ZK 验证          0            ~0.1 s       +0.1 s（EVM 内）
目标分片共识出块          ~10 s        ~10 s         0
──────────────────────────────────────────────────────────
总计                     ~25 s        ~26-27 s     +1-2 s
```

**E2E 延迟增量极小（<10%），主要因为三个开销都被现有流程吸收或并行化。**

---

## 三、吞吐量分析

### 3.1 单池隐私 TPS

```
单个 privacy tx 消息大小估算：
  Groth16 proof：        256 bytes  （2 G1 + 1 G2，固定大小）
  公开输入（6×32B）：    192 bytes
  ElGamal C1, C2：       128 bytes  （2 G1 点）
  AES 密文（Note 明文）：~100 bytes
  Nullifier + new_cm：   64  bytes
  其他 protobuf 字段：   ~100 bytes
  ─────────────────────────────────
  合计：                ~840 bytes  ≈ 1 KB

每块可容纳隐私 tx 数：
  kMaxProposeMsgBytes / 840B ≈ 1,200 txs/block（消息大小限制）
  kMaxTxCount = 20,480（条数上限，不是瓶颈）

单池隐私 TPS：
  1,200 txs / 10 s = 120 TPS / pool
```

### 3.2 系统总隐私 TPS（并行池）

```
单分片（32 池）：120 × 32 = 3,840 TPS
N 分片线性扩展：3,840 × N TPS

实例：
  5  分片 → 19,200  隐私 TPS
  10 分片 → 38,400  隐私 TPS
  20 分片 → 76,800  隐私 TPS
```

### 3.3 与现有方案 TPS 对比

| 方案 | 隐私 TPS | 扩展性 |
|------|---------|-------|
| Tornado Cash（ETH L1） | ~2.5 TPS（受 ETH 30M gas/block 限制） | ❌ 固定 |
| Zcash Sapling（单链） | ~5-10 TPS | ❌ 固定 |
| Monero（环签名） | ~30-50 TPS（受 tx 体积限制） | ❌ 固定 |
| Aztec（L2，Sequencer） | ~100 TPS | ⚠️ 中心化扩展 |
| **Shardora 隐私（10 分片）** | **~38,400 TPS** | ✅ 线性扩展 |

---

## 四、消息大小与存储开销

### 4.1 交易消息大小对比

```
普通跨分片 ToTxMessageItem：
  地址（from, to, base）：  60 bytes
  amount256：               32 bytes
  routing 字段：            ~50 bytes
  合计：                    ~142 bytes

隐私跨分片 ShieldedToTxMessageItem：
  nullifier：               32 bytes
  new_commitment：          32 bytes
  ElGamal C1, C2：         128 bytes
  AES 密文：               ~100 bytes
  Groth16 proof：          256 bytes
  公开输入：               192 bytes
  routing 字段：            ~50 bytes
  合计：                   ~790 bytes
  ────────────────────────
  放大倍数：               ~5.6×
```

每块内隐私 tx 数量上限：1MB / 790B ≈ 1,265 txs（vs 普通 tx 约 7,000 txs）

### 4.2 链上存储开销

| 数据结构 | 大小 | 增长方式 |
|---------|------|---------|
| Merkle 树（Sparse，深度 20） | 初始 0，每笔 tx 增 1 叶节点 × 32B | 线性 |
| Nullifier 集合 | 每笔已花费 tx 32 bytes | 线性 |
| Groth16 Verifying Key（合约内） | ~1-2 KB（一次性部署） | 固定 |
| Merkle 路径（RPC 查询用，链外） | 深度 20 × 32B = 640B / query | 不上链 |

**关键**：Merkle 树叶节点是 Sparse 存储，实际链上存储开销 = `已创建 Note 数 × 32 bytes`，与普通 `_balances` 的每账户 32 bytes 存储量级相同。

---

## 五、主要性能瓶颈与优化路径

### 5.1 瓶颈识别

```
瓶颈 1：移动端证明生成（5-15 s）
  严重程度：⚠️ 用户体验差，但不影响吞吐量
  
瓶颈 2：每块隐私 tx 数量（受消息大小 1MB 限制）
  严重程度：ℹ️ 1,265 txs/block 对大多数场景足够
  
瓶颈 3：Nullifier 集合无界增长（链上存储）
  严重程度：ℹ️ 长期运行后存储膨胀，需定期归档
```

### 5.2 优化方案

#### 优化 A：PLONK/Halo2 替代 Groth16

| 项目 | Groth16 | PLONK/Halo2 |
|------|---------|------------|
| 可信设置 | 电路专属（需仪式） | 通用 SRS（一次性） |
| Proof 大小 | ~256 bytes | ~800 bytes |
| 证明时间 | 1-2 s | 1-3 s |
| 验证时间 | ~100 ms | ~200 ms |
| 递归友好 | ❌ | ✅ |

对于本系统优先考虑 Groth16（proof 最小，链上验证 gas 最低）；若需要递归聚合则切换 Halo2。

#### 优化 B：Groth16 批量聚合验证（同块多 tx 共享验证代价）

```
批量验证 k 个 Groth16 proof：
  成本 ≈ (2+k) 次配对  vs  单独验证 k 个：3k 次配对
  k=10：节省 3×10 - 12 = 18 次配对 → 验证成本降 60%

应用：区块内所有隐私 tx 合并为一次批量验证
结果：单池 120 TPS → 更高（验证不再是瓶颈）
```

#### 优化 C：递归证明聚合（超大吞吐）

```
用户生成单笔 proof（~2s）
Leader 将本块 1,200 个 proof 递归聚合为 1 个 proof（~5s，GPU）
区块只需验证 1 个聚合 proof（~100ms）

效果：
  验证 gas 成本降至 1/1200 每 tx
  单块隐私 tx 容量上限变为消息大小（消息本身），而非验证开销
```

#### 优化 D：移动端证明代理

```
用户 → witness（不含私钥）→ 可信证明代理 → proof
                              ↑
                         零知识：代理知道 witness，但 witness 不含 spending_key
                         spending_key 仅用于计算 nullifier，之后丢弃

成本：证明服务约 1-5 ms 服务器时间 → 可按量收费（<$0.001 / proof）
延迟：网络往返 ~100-500 ms → 用户体验接近零延迟
```

#### 优化 E：Nullifier 归档（链上存储控制）

```
每 N 个 epoch，将已花费 Nullifier 集合做 Merkle 快照
快照哈希写入链上（32 bytes），原始数据移至链外归档
新交易验证：检查当前活跃集 + 检索归档 Merkle 证明

结果：链上 Nullifier 存储量 = 固定窗口大小，不无界增长
```

---

## 六、与现有方案性能全面对比

| 维度 | Tornado Cash | Zcash | Monero | Aztec L2 | Shardora 隐私（基准） | Shardora 隐私（优化后） |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| **E2E 延迟** | ~15 s（L1） | ~75 s（出块） | ~120 s | ~5-30 s | **~27 s** | **~27 s** |
| **客户端证明时间** | ~1 s | ~2-3 s | 无（环签名） | ~1-2 s | **1-2 s（PC）** | **200ms（GPU/代理）** |
| **单链/分片 TPS** | 2.5 | 5-10 | 30-50 | 100 | **3,840 /分片** | **>10,000 /分片** |
| **10 分片总 TPS** | N/A | N/A | N/A | N/A | **38,400** | **>100,000** |
| **Tx 大小** | ~1 KB | ~2 KB | ~2-5 KB | ~1 KB | **~840 B** | **~840 B** |
| **链上存储/Tx** | ~32 B | ~200 B | 链外 | ~32 B | **~64 B** | **~64 B** |
| **证明生成硬件要求** | 低（CPU） | 中（CPU） | 无 | 低 | **中（CPU/GPU）** | **低（代理）** |
| **跨链/跨分片性能** | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ❌ 桥接降级 | ✅ **原生，无额外延迟** | ✅ |
| **吞吐扩展方式** | 不可扩展 | 不可扩展 | 不可扩展 | 受 Sequencer 限制 | **线性（随分片数）** | **线性** |

---

## 七、性能总结

```
关键数字（10 分片，基准配置）：

  E2E 延迟：         ~27 s  （vs 普通跨分片 ~25 s，增量 <10%）
  客户端延迟（PC）：  1-2  s  （可与链上确认并行，不在关键路径）
  系统吞吐量：       38,400 TPS 隐私交易（随分片线性扩展）
  Tx 消息大小：      ~840 bytes（vs 普通 ~142 bytes，5.6× 放大）
  链上存储/Tx：      ~64 bytes（与普通余额账户相当）

性能瓶颈排序：
  1. 移动端证明生成（5-15 s）—— 用代理/GPU 解决
  2. 块内 Tx 容量（受 1MB 消息限制）—— 递归聚合解决
  3. Nullifier 集合增长 —— 快照归档解决

与 Tornado Cash 对比：
  吞吐量提升：15,000×（38,400 vs 2.5 TPS）
  延迟相当：  27 s vs 15 s（略慢，因多了阈值解密）
  金额隐藏：  ✅ 支持任意金额（Tornado Cash 需固定面额）
  跨分片：    ✅ 原生（Tornado Cash ❌）
```

**结论**：延迟代价可忽略（<10%），吞吐量因并行分片呈量级优势。唯一明显代价是 Tx 消息体积放大 ~5.6×，影响单块 Tx 容量上限（1,265 vs ~7,000），但在实际负载下不会成为瓶颈。移动端证明生成是最影响用户体验的问题，可通过证明代理完全解决。

---

*文档版本：2026-09-15*
*基于系统参数：kLeaderRotationPeriodSeconds=10s, kMaxProposeMsgBytes=1MB, kImmutablePoolSize=32*
